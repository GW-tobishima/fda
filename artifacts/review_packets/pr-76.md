# PR-76 Review Packet

## CHANGE_INTENT

#68 FU-002 として、`fda implement` の責務を `lib.rs` から application / rendering module へ分離する。
`lib.rs` は command entry と legacy adapter helper を残し、implement の orchestration と artifact rendering を専用 module に寄せる。

## SCOPE_IN

- `src/application/implement.rs` に implement dry-run / live orchestration、gate 状態確認、Human Decision guard、artifact 出力処理を移動する。
- `src/rendering/implement.rs` に implement handoff、prompt、receipt、inventory の JSON / Markdown 組み立てを移動する。
- `lib.rs` は `application::implement::implement` へ委譲し、review runtime context / guard / role policy の参照先を新 module へ切り替える。
- strict readiness のため、rebase 後に入った `status.rs` の application 境界違反を最小修正する。
- `scripts/check_architecture_boundaries.py` に implement / status の許可境界を明示し、application 層の `crate::infra::` 参照検出を強化する。

## SCOPE_OUT

- review / continue / merge / open / notify の分離は FU-003 以降で扱う。
- Codex / MCP probing など legacy adapter helper の完全分離は FU-003 以降で扱う。
- merge approval、risk approval、scope approval は付与しない。
- UI / frontend / visual surface は変更しない。
- #68 は umbrella EPIC のため、この PR 単体では close しない。

## AC_EVIDENCE

- AC1: `fda implement` の orchestration は `src/application/implement.rs` にあり、`lib.rs` は entry delegation と残存 legacy helper に縮小されている。
- AC2: implement prompt / receipt / inventory rendering は `src/rendering/implement.rs` にあり、direct process / filesystem / clock access を持たない。
- AC3: application 層の direct infra 依存は architecture boundary checker の allowlist で明示され、未許可の `crate::infra::` 参照を検出する。
- AC4: live implement の gate / Human Decision boundary と dry-run artifact output は既存 CLI behavior を保つ。
- AC5: #68 の FU-002 は PR #76 で進行し、FU-003 以降は #68 に残す。

## VALIDATION

- `cargo fmt --all -- --check`
- `cargo check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test -- --test-threads=1`
  - 100 passed
- `python3 scripts/check_architecture_boundaries.py`
- `git diff --check origin/main...HEAD`
- `ATO_CLI_BACKEND=local ATO_DB_PATH=/root/code/forge-delivery-agent/.ato/ato.db ato task readiness --task issue-68-fu002-implement-extract-20260628 --json`
  - verdict: `ready`

## REVIEW_AGENT_GATE

MERGE_APPROVAL: not_granted

| role | status | evidence | rationale |
|---|---|---|---|
| pr_reviewer | REVIEW_AGENT_OK | `review-agent-gate:pr76:pr_reviewer:f60b224-final`; hidden rendering process dependency and uncommitted diff HOLD findings were repaired and rechecked on committed HEAD. | correctness / regression / architecture boundary |
| functional_qa | REVIEW_AGENT_OK | `review-agent-gate:pr76:functional_qa:f60b224-final`; FU-002 AC mapping, CLI behavior preservation, and full Rust validation pass were confirmed. | AC mapping and command behavior |
| security_qa | REVIEW_AGENT_OK | `review-agent-gate:pr76:security_qa:f60b224-final`; rendering layer has no process / infra / direct filesystem access and approval boundaries remain unchanged. | command / filesystem / approval boundary |
| qax2 | REVIEW_AGENT_OK | `review-agent-gate:pr76:qax2:f60b224-final`; ATO broker rejects `forge_reviewer`, so qax2 was used as the AGENTS.md-compliant Forge substitute. | ATO / Forge / FDA review gate consistency |
| design_qa | not_applicable | - | UI / frontend / visual / browser surface の変更なし |

## SECURITY_EVIDENCE

- 新しい network access、credential handling、外部送信は追加していない。
- rendering module は process 実行、filesystem 書き込み、clock access を持たず、application から渡された値だけで artifact を組み立てる。
- live implement の Human Decision guard と dry-run gate の境界は維持している。
- Review Agent Gate の `REVIEW_AGENT_OK` は merge approval ではない。

## TASK_TRACEABILITY

- Issue: https://github.com/msamunetogetoge/forge-delivery-agent/issues/68
- PR: https://github.com/msamunetogetoge/forge-delivery-agent/pull/76
- Branch: `codex/issue68-fu002-implement-extract`
- ATO task: `issue-68-fu002-implement-extract-20260628`
- ATO run: `run_01KW5ZEJXWPPK988Q3PSBGBR6J`
- Implementation commit before this packet: `f60b224`

## HUMAN_DECISIONS_REQUIRED

- HUMAN_TURN_REASON: merge_approval
- REQUESTED_DECISION: CI と Review Agent Gate が通った後に PR #76 を merge 対象にするか。
- RECOMMENDED_OPTION: gate 通過後に merge する。
- WHY_NOW: `REVIEW_AGENT_OK` は merge approval ではないため、最終 merge 判断は別に扱う必要がある。
- IMPACT_IF_DELAYED: #68 FU-002 の `lib.rs` implement 分離が main に反映されない。
- OWNER_ROLE: orchestrator

## OPEN_RISKS

- `lib.rs` には review / continue / merge / open / notify と legacy adapter helper が残る。これは #68 FU-003 以降で分割する。
- FDA repo には `profiles/ato.repo_binding.toml` と Rust readiness wrapper が無いため、標準 wrapper ではなく dedicated worktree 上の manual equivalent checks で検証した。
- #68 は umbrella EPIC のため、PR #76 merge 後も FU-003 以降が残る。

## ROLLBACK_PLAN

- この PR を revert すれば implement module extraction、architecture boundary checker 更新、status readiness repair、review packet を戻せる。
- runtime DB、ATO core schema、Forge core schema は変更していない。
