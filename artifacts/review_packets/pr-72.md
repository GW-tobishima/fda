# PR-72 Review Packet

## CHANGE_INTENT

PR #70 の Codex review comment に対する follow-up として、Rust module の責務境界を整理する。
あわせて、ATO / Forge / FDA を使う開発では Review Agent Gate を必須化し、次回以降の PR で reviewer evidence 欠落を CI で検出できるようにする。

## SCOPE_IN

- `CheckError` を application use case 実装から port boundary へ移動する。
- rendering が application use case DTO へ依存しないよう、共有 DTO を domain entity へ移動する。
- support helper から filesystem IO を外し、JSON file IO と canonicalize を infra 側へ移動する。
- repo-local `AGENTS.md` に ATO / Forge / FDA 開発時の Review Agent Gate 必須化を記録する。
- `scripts/check_review_agent_gate.py` と CI で、PR review packet の `REVIEW_AGENT_GATE` を検証する。
- `artifacts/review_packets/pr-72.md` に reviewer evidence を残す。

## SCOPE_OUT

- 旧 PR #70 の review thread を勝手に resolve / reply しない。
- merge approval、risk approval、scope approval を reviewer に委譲しない。
- ATO / Forge の core state schema を変更しない。
- UI / frontend / visual surface を変更しない。

## AC_EVIDENCE

- AC1: `CheckError` は `src/application/ports.rs` に定義され、validator port contract の error type として扱われる。
- AC2: rendering 層は `application::runtime::RuntimeContext` / `application::start::IntakeInput` に依存せず、domain entity を参照する。
- AC3: `src/support/*` に direct filesystem IO は残らず、JSON file IO は `src/infra/json_file.rs`、canonicalize は `src/infra/paths.rs` に置かれる。
- AC4: ATO / Forge / FDA を使う PR では Review Agent Gate が必須となり、`pr_reviewer` / `functional_qa` / `security_qa` / `forge_reviewer` または `qax2` / `design_qa` not-applicable rationale を review packet と CI で確認する。

## VALIDATION

- `cargo fmt --all --check`
- `cargo check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test -- --test-threads=1`
  - 78 passed
- `cargo run --quiet -- validate-artifacts --artifacts docs/standards/delivery-artifacts-v0/examples/fda_arch_lib_rs_refactor --out /tmp/pr72-review-4586497246-validation_report.json`
  - 56 passed, 0 failed, 32 skipped
- `python3 scripts/check_architecture_boundaries.py`
- `python3 scripts/check_review_agent_gate.py --pr-number 72`
- `python3 -m unittest tests/test_review_agent_gate.py`
  - 15 passed
- `python3 -m unittest discover -s tests`
  - 56 passed
- GitHub checks
  - `rust`: success
  - `GitGuardian Security Checks`: success

## REVIEW_AGENT_GATE

MERGE_APPROVAL: not_granted

| role | status | evidence | rationale |
|---|---|---|---|
| pr_reviewer | REVIEW_AGENT_OK | `review-agent-gate:pr_reviewer:pr72:review-4586497246-final`; duplicate role and MERGE_APPROVAL bypass findings are fixed. | correctness / regression / blast radius |
| functional_qa | REVIEW_AGENT_OK | `review-agent-gate:functional_qa:pr72:review-4586497246-final`; AC1-AC4 evidence, review packet, and 15 gate tests are consistent. | AC mapping and regression evidence |
| security_qa | REVIEW_AGENT_OK | `review-agent-gate:security_qa:pr72:review-4586497246-final`; duplicate role, MERGE_APPROVAL, and placeholder bypasses are fixed. | command / filesystem / approval boundary |
| qax2 | REVIEW_AGENT_OK | `review-agent-gate:qax2:pr72:review-4586497246-final`; AGENTS.md policy, gate checker, review packet, and merge-approval separation are consistent. | ATO / Forge / FDA review gate consistency |
| design_qa | not_applicable | - | UI / frontend / visual / browser surface の変更なし |

## REVIEW_RESPONSE_4586497246

- `scripts/check_review_agent_gate.py` の status 判定を正規化し、`HOLD`、`FAIL`、`PENDING` 系の表記を任意 reviewer でも拒否する。
- `forge_reviewer` / `qax2` に加えて、AGENTS.md で許可している orchestrator review-gate run を forge gate equivalent として扱う。
- `MERGE_APPROVAL` は専用行を parse し、値が `not_granted` 単独である場合だけ通す。
- evidence / rationale の placeholder 判定を正規化し、`N/A`、`None`、`null`、`not_applicable` 系を不足として拒否する。
- 同一 role が複数行ある場合は後勝ち上書きせず、重複として拒否する。
- `MERGE_APPROVAL:` で始まる行はすべて検出し、末尾コメントや追加説明を含む行も拒否する。
- `N/A.`、`None.`、`n.a.` などの区切り・句読点付き placeholder も不足として拒否する。
- `tests/test_review_agent_gate.py` に今回の指摘を固定する回帰テストを追加した。

## SECURITY_EVIDENCE

- 新しい network access、secret handling、authorization bypass は追加していない。
- support helper から filesystem IO を外し、filesystem を扱う関数を infra 側へ寄せた。
- `REVIEW_AGENT_OK` は merge approval / risk approval として扱わない。
- `MERGE_APPROVAL: not_granted` を専用行として検証し、説明文内の文字列では通さない。

## TASK_TRACEABILITY

- PR: https://github.com/msamunetogetoge/forge-delivery-agent/pull/72
- Branch: `codex/pr70-review-followup-boundaries`
- ATO task: `pr70-review-followup-20260628`
- ATO task: `pr72-review-4586497246-followup-20260628`
- Initial implementation commit: `136b093370bd39fd07d078296747ac84d21a478a`

## HUMAN_DECISIONS_REQUIRED

- HUMAN_TURN_REASON: merge_approval
- REQUESTED_DECISION: Review Agent Gate と CI が通った後に PR #72 を merge 対象にするか。
- RECOMMENDED_OPTION: reviewer evidence と CI を確認してから merge 判断する。
- WHY_NOW: `REVIEW_AGENT_OK` は merge approval ではないため、最終 merge 判断は別に残す必要がある。
- IMPACT_IF_DELAYED: PR #70 review follow-up と Review Agent Gate 必須化が main に反映されない。
- OWNER_ROLE: orchestrator

## OPEN_RISKS

- 旧 PR #70 の review thread はこの PR から自動 resolve していない。
- `forge_reviewer` role は現在の ATO broker / transaction policy で直接 run 化できないため、qax2 / orchestrator review-gate evidence で補完する。

## ROLLBACK_PLAN

- この PR を revert すれば、module 境界整理と Review Agent Gate 必須化の CI 追加を戻せる。
- runtime DB / ATO core schema / Forge core schema は変更していない。
