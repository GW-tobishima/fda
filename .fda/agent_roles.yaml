agent_roles:
  default_orchestrator:
    executor: current_claude_code
    workspace_policy: workspace_write
    can_edit: true
    required_checkpoint: true
  implementer:
    executor: current_claude_code
    workspace_policy: workspace_write
    can_edit: true
    role_switch_allowed: true
    required_handoff: implementation_handoff.md
    required_checkpoint: true
  pr_reviewer:
    executor: claude_subagent
    workspace_policy: read_only
    can_edit: false
    required_for_pr: true
  functional_qa:
    executor: claude_subagent
    workspace_policy: read_only
    can_edit: false
    required_for_pr: true
  security_qa:
    executor: claude_subagent
    workspace_policy: read_only
    can_edit: false
    required_for_pr: true
  forge_reviewer:
    executor: claude_subagent
    workspace_policy: read_only
    can_edit: false
    required_when:
      - ato_forge_fda_evidence_changed
      - handoff_changed
      - review_packet_changed
      - human_decision_boundary_changed
    fallback_role: qax2
  design_qa:
    executor: claude_subagent
    workspace_policy: read_only
    can_edit: false
    required_when:
      - ui_surface_changed
      - frontend_changed
      - browser_surface_changed
    not_applicable_requires_reason: true
  merge_manager:
    executor: current_claude_code
    workspace_policy: controlled_write
    can_edit: false
    requires_human_approval: true
